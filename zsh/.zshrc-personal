# My personal stuff
# Generic stuff
alias cp="cp -irv"
alias mkdir="mkdir -pv"
alias rm="rm -iv"

# Kubernetes
alias k="kubectl"
alias kns="kubectl get namespaces"

# zoxide init
eval "$(zoxide init zsh)"
